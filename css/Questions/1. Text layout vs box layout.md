
- What decides whether an element uses **text layout** or **box layout**?
    
- Why are inline elements governed by text layout instead of box layout?
    
- What does it mean that inline elements “cannot disrupt line spacing”?
    
- Why does inline-block suddenly make width and height work?
    
- What exactly changes when flex or grid is applied to a parent?
- What determines whether CSS is positioning an **element box** or **text inside an element**?
    
- Why can a block element be centered by Flexbox while its text remains left-aligned?
    
- What does the text layout engine control that layout systems (flex/grid) never touch?
    
- Why does text-align affect headings and paragraphs even when they are flex items?
    
- In one sentence, how do the responsibilities of the **layout engine** and the **text engine** differ?
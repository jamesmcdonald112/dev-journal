- What does max-content do in CSS Grid, and why can it break responsive layouts?
- What does Grid shrink when space is limited — items or tracks — and how is this different from Flexbox?
- What does minmax(0, 1fr) express about layout priority in CSS Grid?
- In a CSS Grid layout, what does this rule do and why is it considered a robust, future-proof way to span an item across the grid? - grid-column: 1 / -1;
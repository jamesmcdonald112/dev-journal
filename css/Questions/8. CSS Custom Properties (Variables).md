- What does it mean when CSS variables are “scoped”, and how does defining a variable on a parent (e.g. .calculator) affect which elements can use it?
- What happens if a child tries to use var(--pad-x) but none of its ancestors define it?
    
- How does scoping change if the variable is defined on :root instead of a component class?
    
- Why is scoping variables to a component often preferred over global variables?
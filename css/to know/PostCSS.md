It lets you use **extra CSS features or shortcuts** (like custom media queries, nesting, or autoprefixing), then **converts them into normal CSS** that browsers understand.

  

You don’t need PostCSS to build good sites — it’s optional tooling that helps **reduce repetition and improve maintainability**, but it adds setup complexity
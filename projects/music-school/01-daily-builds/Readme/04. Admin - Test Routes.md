Some internal routes are protected via request headers and are not public.

  

Supported authentication methods:

- `Authorization: Bearer <token>` (standard HTTP auth)

- `x-admin-token: <token>` (internal/test usage)

  

These routes are intended for internal testing only.
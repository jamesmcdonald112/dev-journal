Add tests for rate limit + origin validation once implemented

On Vercel, **don’t use in-memory** (serverless instances reset + scale). Use a shared store:

  

**Best default:** **Upstash Redis** (serverless-friendly, common with Vercel).

Alternative: Vercel KV (also fine, similar idea).
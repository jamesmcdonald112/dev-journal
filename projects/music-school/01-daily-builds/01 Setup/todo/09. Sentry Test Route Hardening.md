If keeping it long-term:

- Change to **POST only**
    
- Return **404** when token missing (don’t reveal endpoint)
    
- trim() env token + header token
    
- Add cache-control: no-store
    
- Optional later: restrict by IP
    
    If not keeping:
    
- Delete after final verification